# Pilot broker. Trackers publish with a per-device username/password (see password_file);
# the ingest service subscribes to kumasi/fleet/#. TLS on 8883 is terminated here in production.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
