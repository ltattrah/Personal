// Kumasi Transit tracker-cadence study.
// Self-contained OMNeT++ 6 model (no INET dependency) of GPS trackers reporting
// over a lossy, delayed cellular link to the ingest server. Swap TrackerLink
// for an INET LTE/NB-IoT stack once the INET variant is built (see README).
@namespace(kumasi_cadence);
package kumasi_cadence;
